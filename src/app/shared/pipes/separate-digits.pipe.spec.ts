import { SeparateDigitsPipe } from './separate-digits.pipe';

describe('SeparateDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
