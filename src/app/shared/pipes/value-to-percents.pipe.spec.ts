import { ValueToPercentsPipe } from './value-to-percents.pipe';

describe('ValueToPercentsPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueToPercentsPipe();
    expect(pipe).toBeTruthy();
  });
});
